import classNames from "classnames";

export const DescriptionSection = () => {
  return (
    <section
      className={classNames(
        "grid",
        "grid-cols-12",
        "grid-rows-[345px_366px_366px_510px]",
      )}
    ></section>
  );
};
